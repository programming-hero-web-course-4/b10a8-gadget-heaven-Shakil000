import React from "react";

const Heading = ({ title }) => {
  return (
    <div>
        <h1 className="py-10 px-2 text-center text-3xl font-bold text-violet-500">{title}</h1>
        </div>
  )
  
};

export default Heading;
